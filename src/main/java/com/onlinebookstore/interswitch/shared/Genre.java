package com.onlinebookstore.interswitch.shared;

public enum Genre {
    FICTION, //potential spelling error(Friction) in assessment corrected
    THRILLER,
    MYSTERY,
    POETRY,
    HORROR,
    SATIRE
}
